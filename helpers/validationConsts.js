const REQ_NAME = "Name is required";

const REQ_EMAIL = "Email is required";
const INV_EMAIL = "Invalid email";

const REQ_PASSWORD = "Password is required";

const SUBSCR_STARTER = "starter";
const SUBSCR_PRO = "pro";
const SUBSCR_BUSINESS = "business";

module.exports = {
    REQ_NAME,

    REQ_EMAIL,
    INV_EMAIL,

    REQ_PASSWORD,

    SUBSCR_STARTER,
    SUBSCR_PRO,
    SUBSCR_BUSINESS,
};
