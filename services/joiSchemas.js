// const Joi = require("joi");

// const newContact = Joi.object({
//     name: Joi.string().min(2).max(100).required(),
//     email: Joi.string().email().required(),
//     phone: Joi.string().min(7).max(20).allow(""),
// }).required();

// module.exports = {
//     newContact,
// };
